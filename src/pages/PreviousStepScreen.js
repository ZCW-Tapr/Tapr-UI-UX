// src/pages/PreviousStepScreen.js
//Dummy setup for now
const PreviousStepScreen = () => (
  <div>
    <h2>Step 2: Device Configuration</h2>
    <p>This screen will let users link devices or scan QR codes.</p>
  </div>
);

export default PreviousStepScreen;